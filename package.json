{
  "name": "vasil.ca",
  "version": "1.0.0",
  "description": "Vasil Rimar - Software Engineer from Toronto",
  "scripts": {
    "dev": "next",
    "build": "rimraf build && next build",
    "start": "next start",
    "export": "rimraf out && next export",
    "publish": "node ./scripts/publish.js",
    "deploy": "npm run build && npm run export && npm run publish"
  },
  "author": "Vasil Rimar",
  "license": "MIT",
  "devDependencies": {
    "basic-ftp": "^4.6.3",
    "cross-env": "^7.0.3",
    "next": "^10.0.6",
    "node-sass": "^5.0.0",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "classnames": "^2.2.6",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-ga": "^3.3.0",
    "react-google-recaptcha": "^2.1.0"
  }
}
